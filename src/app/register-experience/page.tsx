"use client";

export default function RegisterExperienceRootPage() {
  console.log("Accessed root")
  return (
    <div className="flex items-center justify-center min-h-screen">
      <span className="body-l text-secondary">Redirecting...</span>
    </div>
  );
}